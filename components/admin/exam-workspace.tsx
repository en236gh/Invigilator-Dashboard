import { Badge } from "@/components/ui/badge";
import type { AllocationStatistics, Examination, ExamVenue, RegisteredStudent } from "@/lib/types/api";
export function ExamWorkspace({exam,students,venues,allocation}:{exam:Examination;students:RegisteredStudent[];venues:ExamVenue[];allocation:AllocationStatistics|null}){return <div className="space-y-6"><section className="rounded-[10px] bg-white p-5 shadow-[0_10px_30px_rgba(15,23,42,0.04)]"><div className="flex justify-between gap-3"><div><h2 className="text-xl font-bold text-ink">{exam.courseCode}</h2><p className="text-sm text-muted">{exam.courseName ?? "Examination session"} · {exam.examDate} · {exam.startTime}–{exam.endTime}</p></div><Badge tone="info">{exam.status}</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><p className="rounded bg-surface-muted p-3 text-sm"><span className="text-muted">Registered</span><br/><b>{allocation?.registeredStudents ?? students.length}</b></p><p className="rounded bg-surface-muted p-3 text-sm"><span className="text-muted">Allocated</span><br/><b>{allocation?.allocatedStudents ?? "—"}</b></p><p className="rounded bg-surface-muted p-3 text-sm"><span className="text-muted">Venue capacity</span><br/><b>{allocation?.totalVenueCapacity ?? "—"}</b></p></div></section><div className="grid gap-6 xl:grid-cols-2"><List title="Assigned venues" empty="No venues assigned." rows={venues.map(v=>`${v.venueName}${v.building?` · ${v.building}`:""} · capacity ${v.capacity??"—"}`)}/><List title="Registered students" empty="No students registered." rows={students.map(s=>`${s.fullName} · ${s.computerNumber}${s.programme??s.program?` · ${s.programme??s.program}`:""}`)}/></div></div>}
function List({title,rows,empty}:{title:string;rows:string[];empty:string}){return <section className="rounded-[10px] bg-white p-5 shadow-[0_10px_30px_rgba(15,23,42,0.04)]"><h2 className="text-lg font-semibold text-ink">{title}</h2><div className="mt-3 max-h-[400px] space-y-2 overflow-auto">{rows.map((r,i)=><p key={i} className="rounded bg-surface-muted px-3 py-2 text-sm text-ink">{r}</p>)}{!rows.length&&<p className="py-8 text-center text-sm text-muted">{empty}</p>}</div></section>}
